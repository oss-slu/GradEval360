`tests/unit/` is for shared schema and utility behavior.

If shared code later participates in broader cross-workspace flows, keep those higher-level tests outside this package's unit folder.

Shared fixtures live in [tests/fixtures/appointments.ts](/Users/premkiran/OSS/GradEval360/tests/fixtures/appointments.ts).
