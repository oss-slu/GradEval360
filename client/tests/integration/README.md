`client/tests/integration/` is reserved for future frontend flows that exercise multiple components together.

Examples: appointment-details page actions, dashboard-to-details navigation, and form submission behavior with mocked API boundaries.
