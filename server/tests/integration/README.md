`server/tests/integration/` is reserved for future API and database workflow tests.

Examples: route authorization, appointment status transitions, seeded-data access rules, and admin sign-off flows against a running server/database.
